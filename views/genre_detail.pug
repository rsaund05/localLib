extends layout

block content

	h1 Genre: #{genre.name}

	div(style='margin-left:20pz;margin-top:20px')

		h4 Books in this genre

		dl

			each book in genre_books
				dt
					a(href=book.url) #{book.title}
				dd #{book.summary}
			else
				p There are no books in this genre.